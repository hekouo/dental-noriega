export const dynamic = "force-dynamic";
export const revalidate = 0;
export const fetchCache = "force-no-store";

import PuntosPageClient from "./ClientPage";

export default function PuntosPage() {
  return <PuntosPageClient />;
}
